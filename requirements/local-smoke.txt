# Install PyTorch separately from the official CUDA 13.0 index:
#   python -m pip install torch==2.13.0 \
#     --index-url https://download.pytorch.org/whl/cu130
transformers==4.57.6
peft==0.18.0
accelerate==1.12.0
tokenizers==0.22.2
safetensors==0.8.0
numpy==2.2.6
